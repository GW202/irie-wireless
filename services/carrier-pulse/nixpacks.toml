[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["python -m venv --copies /app/.venv", ". /app/.venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "cd backend && /app/.venv/bin/uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
